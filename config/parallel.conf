# ViCURA Configuration for Parallel Execution
# Use this configuration with MPI parallel mode

workspace=/mnt/sda1/tempfile_2_28_combine

# Pre-computed OLC input (required for parallel mode)
olc_input_path=/home/qch/OLCtemp/OLC_2_28_combine

# K-mer parameters
minimizer_size=5
kmer_length=150
read_length_normal=150

# Execution mode
use_mpi=true

# Pipeline stages for parallel mode
build_olc=false
read_olc_from_file=true
extract_subgraphs=false
detect_cycles=false
build_dbg=true
parallel_kmer_processing=true
